# FastAPI framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Pydantic for data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# SQLAlchemy for database ORM
sqlalchemy>=2.0.25
sqlalchemy[asyncio]>=2.0.25

# Async PostgreSQL driver
asyncpg>=0.29.0

# Standard PostgreSQL driver (for sync operations if needed)
psycopg2-binary>=2.9.9

# Python environment variables
python-dotenv>=1.0.0

# HTTP client (for potential external API calls)
httpx>=0.26.0

# JWT authentication
python-jose[cryptography]>=3.3.0

# Cloudflare R2 (S3-compatible) client, for presigned uploads
boto3>=1.34.0

# Per-user rate limiting on upload endpoints
slowapi>=0.1.9

# NLP: lemmatization for auto-populating sentence_tokens. Commented out -
# this service runs on a 512MB host and can't afford spaCy/Stanza/hazm's
# model memory footprint at request time. Tokenization now runs locally
# instead, via scripts/reels/backfill_tokens.py (which pulls these same
# packages in scripts/reels/requirements.txt) - re-enable here (and the
# in-process call in reel_creation_service.py) if this service ever moves
# to a host with more RAM to spare.
# spacy>=3.8.0
# stanza>=1.8.0
# hazm>=0.10.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0

# Development tools
black>=24.1.0
isort>=5.13.0
flake8>=7.0.0
mypy>=1.8.0
